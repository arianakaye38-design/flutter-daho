// This file is a placeholder. When you configure Firebase with the
// FlutterFire CLI it will generate a `firebase_options.dart` file.
// Keep this stub so the app still compiles if you haven't added
// Firebase configuration yet.

class DefaultFirebaseOptions {
  static dynamic get currentPlatform => null;
}
